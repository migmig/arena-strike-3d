# Wrangler config for Cloudflare Pages
# Used by `wrangler pages deploy ./dist --project-name=arena-strike-3d`
name = "arena-strike-3d"
compatibility_date = "2026-01-01"
pages_build_output_dir = "./dist"
